package com.uade.TrabajoPracticoProcesoDesarrollo.config;

// NOTE: Disabled to avoid duplicate PasswordEncoder bean; see SecurityConfig.passwordEncoder().
// Keeping class as placeholder to avoid compile issues if referenced elsewhere.
public class SecurityBeansConfig {
}
